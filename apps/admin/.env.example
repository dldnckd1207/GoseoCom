# Admin is served behind the same reverse proxy as the API.
# Leave VITE_PUBLIC_API_URL unset so requests use same-origin paths:
# /admin/api, /api, /auth
#
# Set this only if admin must call a separate API origin directly.
# VITE_PUBLIC_API_URL=https://api.haedok.test.kr

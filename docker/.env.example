APP_VERSION=0.1.0
SERVER_PORT=8000
CLIENT_PORT=3000
# 브라우저에서 접근 가능한 BE 공개 URL
# HTTPS 운영 시 반드시 https://도메인 으로 변경 (Mixed Content 오류 방지)
PUBLIC_API_URL=http://localhost:8000

# 번역 과금 제한 (apps/server/.env에서 설정)
# TRANSLATE_DAILY_LIMIT=10   # 0 = 무제한

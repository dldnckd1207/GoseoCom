# =============================================================================
# Haedok AI — PostgreSQL 커스텀 설정
# 기본값에서 변경이 필요한 항목만 기재합니다.
# =============================================================================

# 로깅
log_timezone = 'Asia/Seoul'
log_min_duration_statement = 1000   # 1초 이상 쿼리 로깅

# 타임존
timezone = 'Asia/Seoul'

# 커넥션
max_connections = 100
